<?php
	
	/* Фильтрация пустых данных */
	/************************************************************/
	foreach ($_REQUEST as $key => $a) 
	{
		$form[$key] = array_diff($a, array('') );

		if ( empty($form[$key]) )
		{
			unset( $form[$key] );
		}
	}
	/************************************************************/

	print_r($form);
?>